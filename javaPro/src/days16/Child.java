package days16;

import days17.Parent;

public class Child extends Parent{

	void modifiersTest() {

		// (조건) 다른 패키지 안(==패키지 외부) - Parent, Child 상속관계

		// 패키지 외부여도 상속관계가 있으므로 protected 필드 보여짐
		// protected = default + 상속관계
		// this.pro 

	}
}